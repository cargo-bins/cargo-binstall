[workspace]
members = [
    "crates/bin",
    "crates/lib",
    "crates/detect-wasi",
    "crates/flock",
    "crates/normalize-path",
    "crates/detect-targets",
]

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = "symbols"
